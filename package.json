{
  "name": "sde1-emi-mysql",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "prisma": "^5.0.0"
  },
  "scripts": {
    "build": "npm --workspace frontend run build",
    "postinstall": "npm --workspace backend run prisma:generate"
  }
}